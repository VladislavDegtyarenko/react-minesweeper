export const CELL_SIZE_REM = 2.125;

export const CELL_SELECTOR = '[data-cell]';

// Base (zoom === 1) pixel values for grid metrics that must visually scale
// with zoom, matching the cell content (see getCellGapVars in utils.ts).
export const CELL_GAP_PX = 2;
export const CELL_HIT_INSET_PX = 1;
export const CELL_BORDER_WIDTH_PX = 1;
