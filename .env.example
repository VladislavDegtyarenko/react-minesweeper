# Supabase config
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Use http://localhost:3000 for local development
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Vercel recommendation:
# - Production: https://minesweeper-classix.vercel.app
# - Preview: https://minesweeper-classix.vercel.app (canonical fallback only)
# Browser auth redirects use the current runtime origin, so preview auth returns
# to the same preview host if Supabase wildcard redirect URLs are configured.
