---
description: Prefer @/ alias imports for src
globs: **/*.{ts,tsx}
alwaysApply: false
---

# Alias Imports

Canonical rule: the `Imports` section of `.codex/rules/code-style.md`.

In short: use `@/` alias imports for anything under `src/`; use relative paths only within a component's own folder.
